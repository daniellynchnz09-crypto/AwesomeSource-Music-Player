<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Scoped-storage folder access to the user's chosen music folder(s), granted
         per-tree via ACTION_OPEN_DOCUMENT_TREE at runtime - no broad storage
         permission is requested. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application
        android:name=".AwesomeSourceApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AwesomeSource"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- TODO(Phase 3): declare the Media3 MediaSessionService here
             (PlaybackService, foregroundServiceType="mediaPlayback", intent-filter
             action androidx.media3.session.MediaSessionService) once the playback
             module is built. Left out for now rather than pointing at a class that
             doesn't exist yet. -->

    </application>

</manifest>
